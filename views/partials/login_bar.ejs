<div v-if="loginbar" class="modal show" tabindex="-1" style="display: block; background-color: rgb(0,0,0,0.5 )">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title">Add Doctor</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close" @click="toggleLoginbar"></button>
      </div>
      <div class="modal-body">
        <select v-model="newShift" class="form-select mb-2" id="shift-select">
          <option disabled value="" selected>Select Start:</option>
          <option v-for="detail in state.shift_details" :value="detail.id">{{ detail.name }}</option>
        </select>

        <div v-if="newShift == 1">
          <div class="bg-warning-subtle p-2 mb-2">
            <p>
              <svg class="octicon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 16 16" width="16" height="16"><path fill-rule="evenodd" d="M8.22 1.754a.25.25 0 00-.44 0L1.698 13.132a.25.25 0 00.22.368h12.164a.25.25 0 00.22-.368L8.22 1.754zm-1.763-.707c.659-1.234 2.427-1.234 3.086 0l6.082 11.378A1.75 1.75 0 0114.082 15H1.918a1.75 1.75 0 01-1.543-2.575L6.457 1.047zM9 11a1 1 0 11-2 0 1 1 0 012 0zm-.25-5.25a.75.75 0 00-1.5 0v2.5a.75.75 0 001.5 0v-2.5z"></path></svg>
              It looks like you are starting the <strong>6 am shift</strong>.</p>
            <p>Do you first want to reset the board and start a fresh rotation?</p>
            <button class="btn btn-danger" @click="resetBoard">Reset Board</button>
          </div>
           <p class="bg-info-subtle p-2 mb-2">If you already reset the board, go ahead and <strong>add a doctor.</strong></p>
        </div>

        <select v-model="newDoctor" class="form-select mb-2" id="shift-select">
          <option disabled value="" selected>Select Doctor:</option>
          <option v-for="d in state.doctors" :value="d.id">{{ d.last }}, {{ d.first }}</option>
        </select>
        
        <button class="btn btn-primary" @click="joinrotation" :aria-disabled="newShift == ''">Add to Rotation</button>
      </div>
    </div>
  </div>
</div>
